(ns schema2
  (:require [schema.core :as s]
            [schema.coerce :as sc]))

; Copy working implementations here when you want to use schemas on frontend

(s/defschema Author {})

(s/defschema Book {})